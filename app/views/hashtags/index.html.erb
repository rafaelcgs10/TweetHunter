<div class="container-fluid">
  <div class="row justify-content-left">
    <p>
      <%= link_to 'New hashtag', new_hashtag_path, class: "btn btn-primary" %>
    </p>
  </div>

  <div class="row">

    <div class="container-fluid">
      <div class="card-deck">
	<div class="row justify-content-left">
	  <% @hashtags.each do |tag| %>
	  <div class="card mb-4" style="min-width: 10rem; max-width: 15rem; min-height: 5rem; max-height: 15rem">
	    <div class="card-body">
	      <h5 class="card-title"><%= tag.hashtag %></h5>
	      <div class="row">
		<div id="show_<%= tag.hashtag %>" class="card-link" >
		  <%= link_to 'Show', hashtag_path(tag) %>
		</div>
		<div class="card-link" id=delete_<%= tag.hashtag %>>
		  <%= link_to 'Delete', hashtag_path(tag), method: :delete,
		      data: { confirm: 'Are you sure?' } %>
		</div>
	      </div>
	    </div>
	  </div>
	  <% end %>
	</div>
      </div>

    </div>
  </div>
</div>
